# UU_CV_Dog_Cat_object_detection
build and train an object detection network in the spirit of YOLOv1 for dog/cat head detection

# install requirements
run npm install with the requirements.txt file in the root of this repo to install all dependencies. You may want to do this in a virtual environment.

```Bash
npm install -r requirements.txt
``` 